# CLAUDE.md

## このフォルダについて

`flutterprojects` は、複数の独立したFlutterアプリプロジェクトを並べて管理する作業ディレクトリです。このディレクトリ自体はGitリポジトリではなく、直下の各サブフォルダ（`Math_Practice` など）がそれぞれ独立したFlutterプロジェクト（多くは個別のGitリポジトリ）です。

## 基本的な起動方法とトリガーワード

このワークスペースでの作業は、必ず次のように `flutterprojects` 直下（またはその配下）で `claude` を起動することから始める。

```
cd flutterprojects && claude
```

CLAUDE.mdはcwdから上位ディレクトリへ再帰的に読み込まれるため、これだけで本ファイルの内容がセッションに読み込まれる。**これ以降、`mkdir`・`flutter create`・作業ディレクトリへの`cd`などの操作を人間に行わせず、Claude自身がBashツールで実行すること。** 「まず`flutter create`しておいてください」のように人間へコマンド実行を求めない。まだ存在しないフォルダを新規作成する場合も同様に、Claude自身が作成してからそこで作業を進める。

人間の依頼に次のキーワードが含まれていたら、対応するフローを該当セクションの手順に従って自律的に開始する。

| トリガーワード | 開始するフロー | 手順の記載箇所 |
|---|---|---|
| 「新規実験」＋アプリ名 | ①実験開発の開始 | 本ファイル「① 実験開発」節 |
| 「構成書作成」＋アプリ名 | ③構成書の初稿作成 | 本ファイル「③ 構成書の作成」節 |
| 「新規プロジェクト作成」／「正規開発開始」＋アプリ名 | 正規開発の開始 | 本ファイル「新規プロジェクト作成時のルール」 |

（②チームレビューは人間同士のミーティングであり、Claudeが開始するフローではないためトリガーワードはない。）

## 主要フォルダの役割

- `projectTemplate/` — **新規プロジェクト作成時に適用する雛形。** `CLAUDE.md`（DRY原則・環境変数管理・テスト駆動・審査対策・Git運用・SOP）、テスト仕様書フォーマット、`lib/utils/_AI_INDEX.md` など、アプリ固有の内容を除いた共通ルール一式が入っている。中身は書き換えず、新規プロジェクトへコピーして使う。
- `apple-reviewer/` — App Store審査対策の参照専用ナレッジベース。独自の `CLAUDE.md` を持ち、審査エージェントとして振る舞うためのフォルダなので、通常のアプリ開発作業では編集しない。
- `experiments/` — **実験開発（後述）専用の置き場。** `experiments/<アプリ名>/` ごとに自由な試作用Flutterプロジェクトを作ってよい試行錯誤領域。通常のプロジェクトルール（DRY原則・テスト仕様書・Git運用等）は適用されない。ここでの成果物は正規開発の土台としてそのまま流用しない（正規開発は必ず `projectTemplate` から作り直す）。
- `composition_docs/` — ③構成書の置き場。雛形は `composition_docs/_TEMPLATE.md`、アプリごとの実体は `composition_docs/<アプリ名>.md`。
- それ以外の直下フォルダ（`Math_Practice` / `AIdea` / `a_icalendar` / `homework_submission` / `flutter` / `fluttermemo` 等） — 個別アプリのプロジェクトフォルダ。各フォルダ内の `CLAUDE.md` がそのプロジェクト自身のルール（`projectTemplate` を適用・カスタマイズしたもの）。

## アプリ開発のフロー（実験 → レビュー → 構成書 → 正規開発）

新規アプリは、いきなり下記「新規プロジェクト作成時のルール」（`projectTemplate` によるGitリポジトリ化）から始めるのではなく、以下の3段階を経てから正規開発に入る。

### ① 実験開発

- 人間から「新規実験」というキーワード（＋アプリ名）を受け取ったら、このフローを開始する。`experiments/<アプリ名>/` が無ければClaude自身が `flutter create` で作成し（人間にコマンド実行を求めない）、以降はそこで作業する。作ってみたい機能を自由に試作するフェーズであり、画面遷移が破綻していても、ロジックが多少不正確でも、App Store審査基準に沿っていなくても構わない。
- 通常のプロジェクトルール（DRY原則、長文命名規則、テスト仕様書の事前作成、Git運用のコミット/pushルールなど）はこのフェーズでは免除される。スピード・気軽さを最優先してよい。
- 唯一の記録義務は、下記 `EXPERIMENT_LOG.md` への記録のみ。

**`experiments/<アプリ名>/EXPERIMENT_LOG.md` の記録ルール**

②のチームレビューで「何を・なぜ試したか」を思い出せるようにするための最低限のメモ。実装コードやファイル名、使用パッケージ名などの技術詳細は書かない（必要ならコード自体を見れば分かるため）。

- 1機能（1アイデア）につき1エントリとする。同じ機能に手を加えた場合は新規追記でなく既存エントリを上書き更新する（時系列の日記にしない）。
- フォーマットは以下。**全項目任意**で、埋められる範囲だけ埋めればよく、機能名だけの1行でも構わない。

  ```
  ## 機能名
  **狙い:** （分かる範囲で1〜2文。聞き出しには行かない）
  **状態:** 採用候補 / 要改善 / 没 （判断できる場合のみ）
  **気づいたこと:** （あれば）
  ```

- **記録のために作業を止めたり、ユーザーに質問したりしないこと。** 会話や実装の流れの中で自然に分かる範囲だけを埋め、実験のテンポを妨げないことを優先する。

### ② チームレビュー

①の実験結果と `EXPERIMENT_LOG.md` をもとに、チームミーティングで実装したい機能をレビューする（実現可能性、機能の詰め込み過ぎ、改善アイデアなど）。

### ③ 構成書の作成

- 人間から「構成書作成」というキーワード（＋アプリ名）を受け取ったら、このフローを開始する。`composition_docs/<アプリ名>.md` が既に存在する場合は無条件に上書きせず、既存内容を踏まえてどう更新するか（新機能の枝を追記する等）を一旦人間に確認してから進める。存在しない場合のみ `composition_docs/_TEMPLATE.md` をコピーして新規作成する。
- 独自の計算ロジックや細かい仕様まで書き込む必要はない。構成書は「完成した仕様書」ではなく、正規開発時にエージェントとの対話で詳細を詰めていくための出発点として扱う。
- 初稿はエージェントが①の `EXPERIMENT_LOG.md` と②のレビュー内容をもとに起こし、人間は内容の要不要を直す程度でよい。
- テンプレートの構成（メタデータ・概要・画面構成の構造木・収集データの事前調査・未確定事項）は `composition_docs/_TEMPLATE.md` 側の定義を正とする。
- メタデータの「プロジェクトフォルダパス」は、この時点で正規開発時の配置先 `flutterprojects/<アプリ名>/` を確定させて記入する（後工程での更新は不要）。

③の構成書がまとまったら、次の「新規プロジェクト作成時のルール」に進み、正規開発を開始する。

### 既存アプリへの機能追加の場合

既存アプリ（各プロジェクトフォルダ）に新機能を追加する場合、この①〜③のフローを踏むかどうかは機能の規模で判断する。

- 既存のSOPで対応できる小さな修正・追加は、①〜③を経由せず各プロジェクトの `CLAUDE.md` のSOPに直接従って着手してよい。
- 新しい画面群やアイデアの検証が必要な大きめの新機能は、まず `experiments/` で試作し、②のレビューを経て、③のフローに進む。「ミニ版」という別形式は設けず、対象アプリの既存 `composition_docs/<アプリ名>.md` があれば新機能を新しい枝として追記する形で更新し、無ければ（構成書導入前からの既存アプリ等）新規作成してから該当プロジェクトへ組み込む。

## 新規プロジェクト作成時のルール（最重要）

①〜③（実験開発・チームレビュー・構成書の作成）を経て正規開発に進む段階になったら、必ず以下の手順で `projectTemplate` を適用すること。人間から「新規プロジェクト作成」「正規開発開始」「新しいアプリを作りたい」等の依頼を受けたら、勝手にゼロから雛形を書き起こさず、このフローに従う。

1. **`flutter create`**
   このディレクトリ（`flutterprojects`）直下でClaude自身が `flutter create <new_project_name>` を実行し、新規プロジェクトフォルダを作成する（人間にコマンド実行を求めない）。`<new_project_name>` は対応する構成書のメタデータ「プロジェクトフォルダパス」に確定済みの名称を用いる。対象フォルダが既に存在する場合は無条件に実行せず、一旦人間に確認する。
2. **`projectTemplate` の読み込み**
   `projectTemplate/CLAUDE.md` と `projectTemplate/README.md` を読み、雛形の内容と適用手順を把握する。
3. **雛形ファイルの適用**
   `projectTemplate/README.md` の「使い方」に従い、以下を新規プロジェクトのルートに新規作成・コピーする（既存の `flutter create` 生成物を上書きしない）。
   - `CLAUDE.md`
   - `.env.example`（コピー後 `.env.prod` を作成し、本番用の実値を記入。`.env.prod` はコミットしない。開発時は `defaultValue` によるフォールバックで素の `flutter run` を使うため別途 `.env` は不要）
   - `docs/test_specs/README.md`・`docs/test_specs/_TEMPLATE.md`
   - `docs/PENDING_DECISIONS.md`
   - `lib/utils/_AI_INDEX.md`
   - `.gitignore` の内容を、`flutter create` 生成分とマージ（`.env`/`.env.prod` 除外ルールを追加）
   - `lib/ui/screens/`・`lib/ui/widgets/`・`lib/utils/` のディレクトリ構成
4. **アプリ固有情報への置き換え**
   コピーしたファイル内のプレースホルダーや説明文（アプリ名・想定機能など）を、新規プロジェクトの内容に合わせて具体化する。共通ルール自体（DRY原則・環境変数の扱い・Gitブランチ運用・SOP等）は変更しない。
5. **Git初期化**
   新規プロジェクトを独立したGitリポジトリとして初期化し（`git init`）、`dev` ブランチで作業を開始する。`main` は安定版ブランチとして扱い、直接コミットしない。`dev`へのコミット後は毎回pushし、リリース確定時に`dev`の最新コミットへfast-forwardして進める（詳細は各プロジェクトの `CLAUDE.md` 5章・7章を参照）。
6. **審査対策ナレッジベースへの登録（該当する場合）**
   将来App Storeへ提出する予定のアプリであれば、`apple-reviewer/apps/{アプリ名}.md` の新規作成が必要になる旨を人間に伝える（審査エージェント自身が作成するため、ここでは提案のみに留める）。新規プロジェクトの `CLAUDE.md` 9章には、この連携ルールが `../apple-reviewer/` を前提として既に組み込まれている。

## 注意事項

- `projectTemplate/` 配下のファイルは全プロジェクト共通の雛形であるため、特定アプリ向けの修正はコピー先（各プロジェクトフォルダ）でのみ行い、`projectTemplate/` 自体は編集しない。ただし、複数アプリに共通する改善点だと判断した場合は、人間に提案した上で `projectTemplate/` 側へ反映してよい。
- `apple-reviewer/` はレビュー専用エージェントの作業領域であり、通常のアプリ実装作業では読み書きしない。
