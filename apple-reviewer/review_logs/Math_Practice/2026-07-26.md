# Math_Practice 審査ログ (2026-07-26)

対象ブランチ: dev（`apps/Math_Practice.md`参照）
※本ログは新テンプレート移行時に旧形式から書き直したもの。提案1・2はナレッジベース整備作業中に副次的に見つかった指摘であり、`checklist/master_checklist.md`に沿った網羅的なレビューはまだ実施していない。

## レビューサマリ（審査エージェント記入・このセクションのみ編集可）

### 提案1: アカウント削除導線の未実装疑い
- 種別: 問題のある設計の修正要求
- 対象: `accountScreen.dart`
- 内容: 「削除」表記は広告削除機能のみで、Firebase Authでのアカウント削除処理が見当たらない
- 根拠: [../../apple_guidelines/05_legal/account_deletion.md](../../apple_guidelines/05_legal/account_deletion.md)（Apple 5.1.1(v)、アカウント作成機能があるアプリはアプリ内削除導線が必須）
- 提案内容: アカウント削除導線を実装する

### 提案2: 環境変数URLのビルド注入未整備 → Remote Config方式への変更で解決
- 種別: 問題のある設計の修正要求
- 対象: `appWideUrlEmailAndProductIdConstants.dart`（`privacyPolicyUrl` `termsOfServiceUrl` `supportEmail` `howToSolveUrl`）
- 内容: 値が`String.fromEnvironment(...)`依存だが、注入用の`--dart-define`ビルドスクリプト/CI設定がリポジトリ内に見当たらない。通常ビルドだと空文字になりプライバシーポリシーリンク切れの恐れ
- 根拠: [../../apple_guidelines/05_legal/privacy_policy.md](../../apple_guidelines/05_legal/privacy_policy.md)
- 追記（網羅的審査で発覚した具体的症状）: `homaPage.dart`の「解き方を学ぶ」ボタンは`howToSolveUrl`を開くが、定数宣言の直上に`TODO: 記事公開後に正しいURLへ置き換える`が残っており、値が空文字の場合ユーザーには「サイトを開けませんでした」という事実上のリンク切れボタンとして見える。根拠に[../../apple_guidelines/02_performance/completeness_crash.md](../../apple_guidelines/02_performance/completeness_crash.md)（プレースホルダー・リンク切れの排除）も追加
- ユーザーとの協議を経た最終方針（2026-07-29追記）: 当初案の「CIスクリプトでの`--dart-define`注入整備」は、コンパイル時定数である以上値変更のたびに再ビルド・再審査提出が必要になり、Webサイト改修等で頻繁に変わりうるこの4値には向かないと判断。**4値ともFirebase Remote Configへ移行**する方針に変更（一般化して[../../my_common_templates/flutter_project_structure.md](../../my_common_templates/flutter_project_structure.md)4番に反映済み）
- 提案内容:
  1. `privacyPolicyUrl` `termsOfServiceUrl` `supportEmail` `howToSolveUrl`の4値をFirebase Remote Configのパラメータとして定義し、アプリ起動時にフェッチする
  2. `String.fromEnvironment(...)`依存はすべて撤去し、`AppConstants`はRemote Config未フェッチ時のフォールバック用デフォルト値（現行の空文字ではなく、実際に機能する初期値）を持たせる
  3. `homaPage.dart`のTODOコメント（`記事公開後に正しいURLへ置き換える`）は、Remote Config側の値更新で解決する運用に切り替わるため、コード側のTODOコメントは撤去する
  4. Remote Configのフェッチ失敗時（オフライン等）にリンク切れ表示にならないよう、フォールバック値の動作を確認するテストケースを追加する

### 提案3: 返金申請ページ（reportaproblem.apple.com）への案内欠如
- 種別: 問題のある設計の修正要求
- 対象: `settingsScreen.dart`の「お問い合わせ」導線
- 内容: 問い合わせ導線はmailtoによる一般窓口（不具合・データ復元依頼向け）のみで、Appleの返金申請ページへの案内文言がどこにも存在しない
- 根拠: [../../refund_cancellation/apple_managed_refund_flow.md](../../refund_cancellation/apple_managed_refund_flow.md)（返金申請ページへの案内・問い合わせ窓口との役割分担の明示が必須）
- 提案内容: 問い合わせ画面に「返金はreportaproblem.apple.comから申請してください」の案内文言と、自社窓口との役割分担の一文を追加する

### 提案4: アプリ本体のPrivacy Manifestが存在しない
- 種別: 問題のある設計の修正要求
- 対象: `ios/Runner/`
- 内容: サードパーティSDK側の`PrivacyInfo.xcprivacy`は各Podに同梱されているが、アプリ本体用の`ios/Runner/PrivacyInfo.xcprivacy`が存在しない
- 根拠: [../../apple_guidelines/privacy_manifest/overview.md](../../apple_guidelines/privacy_manifest/overview.md)（項目4: Flutterプロジェクトでは`ios/Runner/PrivacyInfo.xcprivacy`への配置が必須）
- 提案内容: `ios/Runner/PrivacyInfo.xcprivacy`を新規作成し、アプリ本体が収集するデータ種別（Firestoreに保存するuid・メールアドレス・学習記録等）とNSPrivacyTracking関連項目を申告する。あわせて、マニフェストを同梱していないプラグイン（`path_provider`等）がRequired Reason APIを直接使用していないか、Xcodeのビルド警告で確認する

### 提案5: ATTプロンプトの表示タイミングが早すぎる → プレプロンプト追加で仕上げ
- 種別: 問題のある設計の修正要求
- 対象: `splashScreen.dart`の`_initialize()`
- 内容: 同意画面（ConsentScreen）でのToS/プラポリ同意直後、ユーザーがアプリの価値を何も体験しないうちにATTシステムプロンプトを表示している
- 根拠: [../../apple_guidelines/05_legal/att_tracking.md](../../apple_guidelines/05_legal/att_tracking.md)（項目2: 文脈のあるタイミングでの表示が必須要件、項目3: プレプロンプト自体は許容されるが誤誘導文言は禁止）
- 提案内容（初版、対応済み）: `AppTrackingTransparency.requestTrackingAuthorization()`の呼び出しをsplash画面から外し、**ユーザーが初めて1回分のゲームを完了してresultScreenに到達したタイミング**まで遅らせる。一度だけ表示すればよいので、`AppSettings`に`hasRequestedAtt`のようなフラグを持たせて初回のみ発火させる。ATT結果が出るまでの間（初回プレイ中）は`setPersonalizedAds(false)`を初期値としておく
- ユーザーとの協議を経た追加方針（2026-07-29追記）: resultScreen到達時にシステムダイアログをいきなり呼ぶ構成は、説明がなく唐突（「謎感」）だという指摘を受け、**システムダイアログの直前に独自のプレプロンプト画面を挟む**ことで解決する方針に変更。文言は「学生チームなので応援してください」的な同情訴求は誘導とみなされるリスクがあるため不採用とし、トラッキングの目的を正確に説明しつつコントロール感を伝える内容で確定した。
- 確定したプレプロンプト文言:
  - タイトル: 「より快適に使うために」
  - 本文: 「他のアプリやサイトをまたいだ行動データをもとに、あなたに合った広告を表示するために使用します。許可しない場合も、アプリの機能は変わらずお使いいただけます。この設定はあとから端末の設定でいつでも変更できます。」
  - ボタン: 「次へ」（タップ後にOSのATTシステムダイアログを表示）
- 追加の提案内容:
  1. `resultScreen.dart`の`_maybeRequestTrackingAuthorization()`内、`AppTrackingTransparency.requestTrackingAuthorization()`を呼ぶ直前に、上記文言のプレプロンプトダイアログ（またはボトムシート等、既存UIコンポーネントの慣習に合わせる）を表示する
  2. プレプロンプトで「次へ」が押された場合のみシステムダイアログを呼ぶ。プレプロンプトを閉じる/キャンセルする導線がある場合、その場合の`hasRequestedAtt`の扱い（再度次回起動時に聞き直すか、拒否扱いにするか）は設計判断が必要なため、作業エージェントは独断で決めず確認すること
  3. `docs/test_specs/consent_and_tracking_authorization.md`にプレプロンプト表示のテストケースを追加する

### 提案6: インタースティシャル広告の表示頻度に上限がない
- 種別: 問題のある設計の修正要求
- 対象: `resultScreen.dart`（`AdManager.instance.showInterstitial`呼び出し箇所）
- 内容: リザルト画面に遷移するたびに毎回インタースティシャルが表示され、頻度キャップが存在しない
- 根拠: [../../advertising/ad_placement_ux_rules.md](../../advertising/ad_placement_ux_rules.md)（項目2: 短時間での連続表示を避け、区切りの良いタイミングでのみ表示する）
- 提案内容: `AdManager`に前回表示時刻を記録し、**前回のインタースティシャル表示から90秒未満の場合は今回はスキップする**（`onDismissed`コールバックは表示スキップ時も通常どおり呼び出し、画面遷移自体は妨げない）というクールダウンを実装する

### 提案7: アカウント削除時にpurchase_indexの逆引きエントリが孤立する
- 種別: 問題のある設計の修正要求
- 対象: `functions/src/index.ts`の`deleteAccount`
- 内容: `users/{uid}`配下は`recursiveDelete`で削除されるが、`purchase_index/{indexId}`にある当該ユーザーの購入トークン逆引きエントリは削除されず孤立データとして残る
- 根拠: [../../data_management/database_delete_policy.md](../../data_management/database_delete_policy.md)（項目2: アカウント削除時の関連データ削除範囲を明文化・網羅する）
- 提案内容: `deleteAccount`実行時に、該当uidに紐づく`purchase_index`のエントリも合わせて削除する（uidからの逆引きが必要なら、事前に`purchaseToken -> uid`と逆に`uid -> purchaseToken`の対応も持たせるか、uidでクエリして削除する）

### 提案8: FirebaseAnalytics/GoogleAppMeasurement (12.13.0) にPrivacyInfo.xcprivacyが未同梱
- 種別: 問題のある設計の修正要求（アプリ側では解決不能、SDKアップデート待ちの監視事項）
- 対象: 依存SDK `FirebaseAnalytics` / `GoogleAppMeasurement` バージョン12.13.0
- 内容: 提案4の調査中に判明。他のFirebaseモジュール（Auth/Firestore/Crashlytics等）は`PrivacyInfo.xcprivacy`を同梱済みだが、このバージョンのFirebaseAnalytics/GoogleAppMeasurementには同梱されていない。Xcodeでのアーカイブ・Validate時にwarning/errorとなる可能性がある
- 根拠: [../../apple_guidelines/privacy_manifest/sdk_support_status.md](../../apple_guidelines/privacy_manifest/sdk_support_status.md)（項目2: Firebase各モジュールの対応バージョン確認、項目4: 未対応SDKへの対応方針）
- 提案内容: Firebase SDKの新しいバージョンで対応済みかを定期的に確認し、対応バージョンがリリースされ次第アップグレードする。リリース直前は実際にXcodeでアーカイブ・Validateを行い、Privacy Manifest関連の警告が出ないことを確認する

### 提案9: dart-define値の設計見直し（死んだキー削除・不要な直書き化・安全なデフォルト値）
- 種別: 問題のある設計の修正要求（ビルドコマンド設計に向けた事前整理。網羅的レビューではなく、ビルドコマンド確定作業中に偶発的に見つかった指摘）
- 対象: `.env.dev` `.env.prod` `.env.example`、`lib/utils/adMobBannerInterstitialAndRewardedAdManager.dart`、`lib/main.dart`
- 内容:
  1. Remote Config移行（提案2）により、`.env.dev`/`.env.prod`/`.env.example`内の`PRIVACY_POLICY_URL` `TERMS_OF_SERVICE_URL` `SUPPORT_EMAIL` `HOW_TO_SOLVE_URL`の4キーがどこからも参照されず残存している
  2. Android向け広告ユニットID3種は`.env.dev`/`.env.prod`どちらもGoogle公式の公開テスト用ID（`ca-app-pub-3940256099942544/...`）のままで、本番向けの実IDが未設定（Androidリリースを見据えていないため現状は実害なしとユーザー確認済み。当面対応不要）
  3. 広告ユニットID6種・`GOOGLE_SIGN_IN_SERVER_CLIENT_ID`いずれも`String.fromEnvironment(key)`のみでデフォルト値未指定のため、dart-defineを渡さない素のビルドでは空文字になり広告読み込みが失敗しうる
- 根拠: [../../apple_guidelines/02_performance/completeness_crash.md](../../apple_guidelines/02_performance/completeness_crash.md)（動作不良状態の排除）、[../../my_common_templates/flutter_project_structure.md](../../my_common_templates/flutter_project_structure.md)3番（環境で値が変わらないなら直書き、dart-define値には安全なdefaultValueを設定）
- 提案内容:
  1. `.env.dev` `.env.prod` `.env.example`から不使用の4キー（`PRIVACY_POLICY_URL` `TERMS_OF_SERVICE_URL` `SUPPORT_EMAIL` `HOW_TO_SOLVE_URL`）を削除する
  2. Android向け広告ユニットIDは当面現状維持（対応不要、ユーザー承認済み）
  3. 広告ユニットID6種は`String.fromEnvironment(key, defaultValue: '<Googleの公開テスト広告ID>')`にし、dart-define未指定時は安全なテスト広告にフォールバックするようにする
  4. `GOOGLE_SIGN_IN_SERVER_CLIENT_ID`はdev/prodでFirebaseプロジェクトを分けておらず値が変わらないため、`String.fromEnvironment`をやめて直書き定数に変更し、`.env.dev`/`.env.prod`/`.env.example`から当該キーを削除する

### 提案10: `.env.dev`が冗長になったため削除
- 種別: 新規共通設計提案（ビルドコマンド設計中に偶発的に判明。正式なチェックリストレビューではない）
- 対象: `.env.dev`、および同ファイルを参照している`.gitignore` `CLAUDE.md` `docs/test_specs/ad_management.md` `docs/test_specs/select_mode_screen.md`
- 内容: 提案9で広告ユニットID6種に`defaultValue`（Googleの公開テスト広告ID）を設定した結果、`.env.dev`の中身（全キーがテストID）とコード側の`defaultValue`が完全に一致するようになった。つまり`--dart-define-from-file=.env.dev`を渡すのと、何も渡さず素でビルドするのとで結果が同じになり、`.env.dev`自体の存在意義がなくなった
- 根拠: [../../my_common_templates/flutter_project_structure.md](../../my_common_templates/flutter_project_structure.md)3番（dart-define値には安全なdefaultValueを設定する方針。ユーザー承認済み）
- 提案内容:
  1. `.env.dev`ファイルを削除する
  2. `.gitignore`から`.env.dev`の記載を削除する（`.env.prod`の記載は残す）
  3. `CLAUDE.md` `docs/test_specs/ad_management.md` `docs/test_specs/select_mode_screen.md`内の`.env.dev`への言及を、「開発時は素の`flutter run`でよい（Googleテスト広告IDがdefaultValueとして自動的に使われる）」という趣旨に書き換える
  4. `.env.example`は`.env.prod`作成時のテンプレートとして引き続き必要なため残す

### 決定事項の記録: App Store Connect「Appのプライバシー」申告内容
- 種別: 決定事項の記録（コード修正を伴わない、App Store Connect上の申告作業。対応サマリでの追跡は不要）
- 背景: 提出準備中、App Store Connectから「`NSUserTrackingUsageDescription`が含まれるためトラッキングに使用するデータ種別を申告してください」という警告が出たことをきっかけに、「Appのプライバシー」欄全体（収集データ種別・利用目的・個人情報への関連付け・トラッキング目的か否か）を実装調査に基づいて確定した
- 根拠: [../../apple_guidelines/05_legal/data_use_disclosure.md](../../apple_guidelines/05_legal/data_use_disclosure.md)（項目1: 申告と実装の一致が必須、不一致はアカウント停止リスクもある）。[../../apps/Math_Practice.md](../../apps/Math_Practice.md)・[../../my_common_templates/privacy_manifest/xcprivacy_declaration_pattern.md](../../my_common_templates/privacy_manifest/xcprivacy_declaration_pattern.md)（`PrivacyInfo.xcprivacy`側の申告内容との整合を確認しながら判定）
- 確定した申告内容（データ種別 / 利用目的 / 個人情報への関連付け / トラッキング目的か）:
  1. メールアドレス / アプリの機能 / 関連付けあり / トラッキングでない
  2. 名前（`display_name`: Apple提供の氏名→Googleアカウント表示名→メールアドレスのローカル部の優先順で決定。`playerName`: ゲーム記録のプレイヤーラベル） / アプリの機能 / 関連付けあり / トラッキングでない
  3. ユーザID（Firebase Auth uid） / アプリの機能 / 関連付けあり / トラッキングでない
  4. デバイスID（AdMob広告ID） / サードパーティ広告 / 関連付けあり / **トラッキングである**
  5. 購入（`is_premium`・購入トークン） / アプリの機能 / 関連付けあり / トラッキングでない
  6. ゲームプレイコンテンツ（プレイヤー名・モード・グリッドサイズ・スコア・誤答数等の記録） / アプリの機能 / 関連付けあり / トラッキングでない
  7. 製品の操作（Firebase Analyticsイベント） / アナリティクス / **関連付けあり**（`analytics.setUserId(uid)`をコードで確認済み） / トラッキングでない（Google Ads連携が現状未実施のため。連携開始時は要見直し、下記参照）
  8. 広告データ（AdMobの広告視聴・クリック等） / サードパーティ広告 / 関連付けあり / **トラッキングである**
  9. クラッシュデータ（Firebase Crashlytics） / アプリの機能 / **関連付けなし**（`setUserIdentifier`等の紐付けコードが存在しないことをコードで確認済み） / トラッキングでない
  10. 健康・フィットネス／財務情報／位置情報／機密情報／連絡先（アドレス帳）／メール本文／写真動画／音声／閲覧履歴／検索履歴／周囲／身体 → いずれも非該当（該当パッケージ・機能が存在しないことを確認済み）
  11. カスタマーサポートデータ → 非該当と判断（`mailto:`リンクでOSのメールアプリに処理を渡すのみで、アプリ・Firestoreにはデータが渡らない実装のため）
- 運用上の注意（今後の見直しが必要になるケース）: 現在Google Ads（広告出稿プラットフォーム、AdMobとは別製品）とFirebase Analyticsの連携は未実施のため項目7は「トラッキングでない」としたが、**将来Google Ads連携を開始した場合はこの申告を見直す必要がある**（項目7のトラッキング目的を「はい」に変更し、利用目的に「サードパーティ広告」または「デベロッパの広告またはマーケティング」を追加する等）

### 提案11: iPhone専用（iPad非対応）への変更
- 種別: ユーザー依頼によるビルド設定変更（Apple公式ルール違反ではなく、製品判断としての対象デバイス変更）
- 対象: `ios/Runner.xcodeproj/project.pbxproj`（`TARGETED_DEVICE_FAMILY`）、`ios/Runner/Info.plist`（`UISupportedInterfaceOrientations~ipad`）
- 内容: 現在`TARGETED_DEVICE_FAMILY`がDebug/Release/Profileの3構成すべてで`"1,2"`（iPhone+iPad）になっており、iPadでもインストール可能な状態。ユーザーの希望はiPhone専用でのリリース
- 提案内容:
  1. `TARGETED_DEVICE_FAMILY`を3構成すべてで`"1"`（iPhoneのみ）に変更する
  2. `Info.plist`の`UISupportedInterfaceOrientations~ipad`キー（iPad専用の画面回転設定）は対象外デバイスとなり不要になるため削除する
  3. まだ一度も提出していない（「提出後の審査結果: 未提出」）ため、既存iPadユーザーへの互換性は考慮不要
- 備考: Apple公式ガイドライン上iPad対応は必須ではなく、これは製品判断。`my_common_templates/`への一般化は行わない（アプリごとに対象デバイスの判断が異なるため）

### 提案12: 本番候補ビルドがApp Store Connect検証で「Invalid executable」により拒否される
- 種別: 問題のある設計の修正要求（ビルド・パッケージング上の不具合。Apple公式ガイドライン違反というより技術的な検証エラー）
- 対象: `ios/Runner.xcodeproj`（Runnerターゲットのビルドフェーズ）
- 内容: Transporterでのアップロード時に以下のエラーで拒否された。`Invalid executable. The "Runner.app/Frameworks/objective_c.framework/objective_c" executable references an unsupported platform in the x86_64 slice. Since your app targets the iOS platform, it should only include executables that support the iOS platform. Simulator platforms aren't permitted.`（ID: cb1b3578-14f1-45b7-b558-80f670c463c8）
- 調査結果（初版）: `objective_c.framework`は`cloud_firestore`が依存するgRPC-Core（1.69.0）のObjective-Cラッパー部分に由来する。実際にアーカイブ済みバイナリを`lipo -info`で確認したところ、デバイス向けReleaseアーカイブであるにもかかわらず`x86_64 arm64`の両方のスライスが混入していた（本来はarm64のみであるべき）。既存の`Podfile`の`post_install`フック（`flutter_additional_ios_build_settings`のみ）ではこの問題に対処できていない
- 提案内容（初版、対応済みだが不十分と判明）:
  1. Runnerターゲットに、「Embed Frameworks」ビルドフェーズの後に実行されるRun Scriptビルドフェーズを新規追加する
  2. スクリプト内容: 埋め込み済みの各`.framework`のバイナリを走査し、x86_64スライスが含まれていれば`lipo -remove x86_64`で除去した上で、埋め込み済みフレームワークとして再署名する
  3. 修正後、`flutter build ipa --dart-define-from-file=.env.prod`で本番候補ビルドを再生成し、`objective_c.framework`を含む全フレームワークを`lipo -info`で検証し、シミュレータ用アーキテクチャが含まれていないことを確認する
  4. 可能であれば、実際にTransporterでの検証（またはCLIでの相当する検証）が通ることまで確認する
- 再現・追加調査（2026-08-10追記）: 上記対応後の`.ipa`を再度Transporterでアップロードしたところ、今度は`references an unsupported platform in the **arm64** slice`（ID: 8b9c9ed1-17be-4539-b613-9facc3247b6b）で再度リジェクトされた。審査エージェント側で実バイナリを`vtool -show-build`で確認したところ、`platform IOSSIMULATOR`と判明。**`lipo -info`はCPUアーキテクチャしか見ておらず、Apple Siliconのシミュレータはarm64で動作するため「arm64だから安全」とは言えなかった**。x86_64除去は症状の一部を消しただけで、残ったarm64スライス自体がそもそもシミュレータ向けビルドだった（＝根本原因は未解決）。原因として、本セッション内で直前まで`flutter build ios --simulator`によるシミュレータ向けビルドを複数回実行しており、そのビルドキャッシュ（DerivedData/Podsのビルド成果物）がデバイス向けアーカイブ時に誤って再利用された可能性が高い
- 根拠: [../../my_common_templates/flutter_project_structure.md](../../my_common_templates/flutter_project_structure.md)8番（訂正済み。`vtool`での platform 確認とクリーンビルドの必要性を追記）
- 追加の提案内容（2026-08-10追記）:
  1. `flutter clean`を実行し、必要であれば`ios/Pods`削除・関連DerivedData削除・`pod install`のやり直しまで行い、gRPC-Core（`objective_c`含む）を含む全Podsをデバイス向けにクリーンな状態から再ビルドする
  2. `flutter build ipa --dart-define-from-file=.env.prod`で本番候補ビルドを再生成する
  3. 検証は`lipo -info`だけでなく、必ず`vtool -show-build`（または`otool -l`の`LC_BUILD_VERSION`）で`objective_c.framework`を含む全フレームワークの`platform`が`IOS`（`IOSSIMULATOR`ではない）であることを確認する
  4. 前回追加したRun Scriptビルドフェーズ（x86_64除去）はそのまま保険として残してよい（クリーンビルドが根本対処、Run Scriptは万一の再発時の保険という位置づけ）

## 対応サマリ（作業エージェント記入・レビュー対象アプリ側で追記）

### 提案1への対応
- 状態: 誤検知（対応不要）
- 対応内容: 実装は`accountScreen.dart`ではなく`settingsScreen.dart`（再認証 → Cloud FunctionsでのFirestore再帰削除 → Firebase Auth削除）に既に存在していた
- 追加で見つかった問題: 匿名ユーザーにも削除ボタンが表示され、`currentUser.providerData.first`が空リストで例外落ち（クラッシュ）するリスクがあった。Apple 5.1.1(v)は能動的にアカウントを作成したユーザーが対象であり、UI操作を伴わない`signInAnonymously()`のみの匿名ユーザーはそもそも対象外と判断
- 対応内容（追加分）: `settingsScreen.dart`の削除ボタン表示条件を`currentUser != null && !currentUser.isAnonymous`に修正し、匿名ユーザーには非表示に変更。クラッシュ防止とApple 5.1.1(v)スコープの適正化を同時に解決
- 関連コミット: `f3b72c86 chore(ai-save): 匿名ユーザーへのアカウント削除ボタン表示によるクラッシュを防止`
- 反映状況: 反映済み（2026-07-26, `my_common_templates/data_management/data_storage_pattern.md` 4番）

### 提案2への対応
- 状態: 対応済み
- 対応内容:
  1. `pubspec.yaml`に`firebase_remote_config: ^6.4.0`を追加（既存の`firebase_core: ^4.7.0`制約と衝突せず、`firebase_remote_config`解決版のみ追加され他のFirebaseパッケージのバージョンは変わらないことを確認済み）。iOS側は`pod install`で`FirebaseRemoteConfig`ポッド統合済み
  2. `lib/utils/remoteConfigUrlEmailValuesFetchService.dart`を新規作成し、`RemoteConfigService`シングルトンとして実装。`main()`（`lib/main.dart`）で`Firebase.initializeApp()`直後、`runApp()`より前に`initialize()`を呼ぶ設計にした。理由: `ConsentScreen`（初回起動時にプラポリ・利用規約リンクを表示）が`SplashScreen`の処理より早い段階で到達しうるため。`initialize()`内では`setConfigSettings()`（`fetchTimeout: 10秒`, `minimumFetchInterval: 1時間`）→`setDefaults()`（同期的に即時反映、これ以降は常に有効な値を返せる）→`fetchAndActivate()`（例外はcatchして握りつぶす）の順で実行する構成とし、フェッチの成否によらずアプリの起動をブロックしない
  3. Remote Configのパラメータキーは、既存の`--dart-define`キー名をそのまま踏襲（`PRIVACY_POLICY_URL` / `TERMS_OF_SERVICE_URL` / `SUPPORT_EMAIL` / `HOW_TO_SOLVE_URL`）
  4. フォールバックデフォルト値は、リポジトリ内の`.env.dev`/`.env.prod`（Git管理外だが対象プロジェクトのローカルに実在）に既に設定されていた実値をそのまま採用: `PRIVACY_POLICY_URL=https://zetaiw.com/index.html` / `TERMS_OF_SERVICE_URL=https://zetaiw.com/terms.html` / `SUPPORT_EMAIL=developmen.support@gmail.com` / `HOW_TO_SOLVE_URL=https://note.com/developmen_app`。空文字での放置ではないことを確認するため、実装前にWebFetchで実URLの到達性を確認済み（プラポリ・note.comとも実際に公開済みの内容が確認でき、404やプレースホルダーではなかった）。推測での穴埋めは行っていない
  5. `lib/utils/appWideUrlEmailAndProductIdConstants.dart`の`AppConstants`から4値の`String.fromEnvironment(...)`を撤去し、`RemoteConfigService.instance`への委譲getterに変更。`howToSolveUrl`直上にあった`TODO: 記事公開後に正しいURLへ置き換える`コメントは、以後はRemote Config側の値更新で運用する方針のため削除
  6. `docs/test_specs/consent_and_tracking_authorization.md`（プラポリ/利用規約リンクの主管仕様書として代表検証。オフライン起動時のフォールバック正常系・フェッチ成功時の値反映）と`docs/test_specs/settings_screen.md`（お問い合わせ/プラポリ/利用規約リンクの導線、consent側への相互参照）にテストケースを追加。`homaPage.dart`の「解き方を学ぶ」リンク（`howToSolveUrl`）専用の既存テスト仕様書は見つからなかったため新規作成はせず、consent側の代表テストケースの説明文中で同一サービスを参照する旨を明記するに留めた（提案の指示が「関連する既存ファイルを探して追記」だったため新規ファイル作成は範囲外と判断）
  7. `flutter analyze`を修正前後で実行し、修正後も新規issueがないことを確認済み（既存の無関係な警告1件＝`pubspec.yaml`の空`assets:`フィールドのみ残存。今回の変更とは無関係で修正前から存在）
  8. `pod install`をiOS側で実行し、`FirebaseRemoteConfig`ポッドが他のFirebaseポッド（12.13.0系）と衝突なく統合されることを確認済み
- Firebase Consoleへの実パラメータ値登録（本番運用のURL/メールアドレス設定）は本対応のスコープ外のコンソール操作のため未実施。現状はコード側のフォールバック値（4番参照）がそのまま使われる状態であり、Console側で値を設定するまでは実質的に旧`.env.prod`と同じ値が使われる（未設定によるリンク切れにはならない）
- 確認したい点: なし（設計判断で迷った点はなく、審査ログの合意内容どおりに実装した）
- 追加で見つかった問題: なし
- 関連コミット: `47b04841 chore(ai-save): プラポリ/利用規約/サポートメール/howToSolveUrlをFirebase Remote Config化`
- 反映状況: 反映済み（2026-07-29、`my_common_templates/flutter_project_structure.md` 4番）※一般化自体はコード実装前の協議段階で先行済み

### 提案3への対応
- 状態: 対応済み
- 対応内容:
  1. `lib/ui/screens/settings/settingsScreen.dart`の既存「お問い合わせ」ListTileの`subtitle`を「ご意見・不具合・データ復元のご依頼（返金のお申し込みはApple公式窓口をご利用ください）」に変更し、役割分担を明示
  2. 同じ「このアプリについて」カード内に新規ListTile「返金のお申し込み」（subtitle:「Apple公式ページから返金を申請できます」）を追加。タップで`AppConstants.appleReportAProblemUrl`（`https://reportaproblem.apple.com`）を既存の`_launchExternalUrl`で外部ブラウザ表示
  3. `lib/utils/appWideUrlEmailAndProductIdConstants.dart`に固定定数`appleReportAProblemUrl`を追加（不変のApple公式URLのため環境変数化せず直書き）
  4. `docs/test_specs/settings_screen.md`の「外部リンク」テストケースに「返金のお申し込み」を追加し、「6. 審査・コンプライアンス確認」に問い合わせ窓口とApple返金窓口の役割分離を確認する項目を追加
  5. `flutter analyze`で対象2ファイルにissueがないことを確認済み
- 確認したい点（解消済み）: 前回記載した(a)文言のトーン (b)UI配置 (c)テスト仕様書承認ゲートの3点は、いずれもユーザーが提示案通り承認したため解消済み。承認内容どおり「両方採用」（既存subtitleへの一文追加＋新規独立ListTile追加）で実装した
- 追加で見つかった問題: なし
- 関連コミット: `148f1276 chore(ai-save): 設定画面に返金申請ページ（reportaproblem.apple.com）への案内を追加`
- 反映状況: 反映済み（2026-07-26, `my_common_templates/refund_cancellation/support_flow_pattern.md` 1番）※コード修正自体は未着手だが、あるべき設計として先に一般化済み

### 提案4への対応
- 状態: 対応済み
- 対応内容:
  1. `ios/Runner/PrivacyInfo.xcprivacy`を新規作成し、Runnerターゲットの`project.pbxproj`（PBXFileReference / PBXBuildFile / Resourcesビルドフェーズ / トップレベルgroup）に追加。シミュレータ向け`xcodebuild`で、生成された`Runner.app`直下に本ファイルが正しくコピーされることをビルド確認済み
  2. `NSPrivacyTracking`はユーザー確認の結果`true`で確定。判断根拠: 本アプリはATT許可結果に応じてAdMobのパーソナライズ広告有無を切り替えており、Google Mobile Ads SDK自身のマニフェストでもDeviceID項目についてTracking=trueと申告済みで、Apple社のtracking定義に該当すると判断
  3. `NSPrivacyTrackingDomains`はユーザー確認の結果、空配列のまま確定。根拠: アプリ自身のコードは直接トラッキング用ドメインに通信せず、全てGoogle Mobile Ads SDK経由であり、SDK側が使用するドメインはSDK同梱のPrivacyInfo.xcprivacyで既に申告済みのため、アプリ側で重複させない
  4. `NSPrivacyCollectedDataTypes`は前回ドラフトどおり、アプリ本体がFirestoreに保存するデータ（Firebase Auth uid → User ID、メールアドレス → Email Address、学習記録 → Other User Content。いずれもLinked=true、Tracking=false、Purpose=AppFunctionality）を申告
  5. `NSPrivacyAccessedAPITypes`は前回調査どおり本アプリでは不要と判断し省略（Runner自身のコード・使用中の全プラグイン/SDKは各々自身のPrivacyInfo.xcprivacyを既に同梱済みのため）
  6. `docs/test_specs/consent_and_tracking_authorization.md`の「6. 審査・コンプライアンス確認」に、`NSPrivacyTracking=true`・`NSPrivacyTrackingDomains`空配列の判断根拠を追記
  7. `plutil -lint`で生成したplistの構文が正しいことを確認済み
- 確認したい点（解消済み）: 前回記載した`NSPrivacyTracking`のtrue/false、`NSPrivacyTrackingDomains`の要否・内容の2点は、いずれもユーザーが提示案（true / 空配列）どおり承認したため解消済み
- 追加で見つかった問題: `FirebaseAnalytics`（12.13.0）/`GoogleAppMeasurement`（12.13.0）にPrivacyInfo.xcprivacyが未同梱である件は、審査エージェント側で提案8として新規記録済みのためここでは重複記載しない
- 関連コミット:
  - `9e22a8a8 chore(ai-save): アプリ本体のPrivacy Manifest(PrivacyInfo.xcprivacy)を追加`
  - `3baf15ef chore(ai-save): テスト仕様書にPrivacy ManifestのNSPrivacyTracking申告を追記`
- 反映状況: 反映済み（2026-07-29、`my_common_templates/privacy_manifest/xcprivacy_declaration_pattern.md`新規作成）

### 提案5への対応
- 状態: 対応済み
- 対応内容（初版）:
  1. `lib/utils/userAppSettingsSharedPreferencesManager.dart`に永続化フラグ`hasRequestedAtt`（getter）と`setHasRequestedAtt(bool)`を追加
  2. `lib/ui/screens/splash/splashScreen.dart`から`AppTrackingTransparency.requestTrackingAuthorization()`の呼び出しを削除。代わりに、`hasRequestedAtt`が`false`（未要求）なら`AdManager.instance.setPersonalizedAds(false)`を安全なデフォルトとして設定し、`true`（要求済み）ならダイアログを出さない`trackingAuthorizationStatus`で現在の許可状態のみを問い合わせて反映するよう変更
  3. `lib/ui/screens/game/resultScreen.dart`の`initState()`に`_maybeRequestTrackingAuthorization()`を追加。`Platform.isIOS`かつ`hasRequestedAtt`が`false`の場合のみATTダイアログを要求し、結果を`AdManager.setPersonalizedAds`に反映したうえで`setHasRequestedAtt(true)`を永続化する
  4. `docs/test_specs/consent_and_tracking_authorization.md`を新しいタイミング設計に合わせて全面改訂（正常系・異常系にresultScreen到達時の要求、2回目以降の起動での状態反映、端末設定変更時の反映などのケースを追加）。`docs/test_specs/result_screen.md`・`docs/test_specs/ad_management.md`・`docs/test_specs/splash_and_launch_screen.md`にも相互参照と該当ケースを追記
  5. `flutter analyze`で対象ファイルにissueがないことを確認済み
- 追加で見つかった問題: なし
- 関連コミット: `b4c2217f chore(ai-save): ATT許可ダイアログの要求を初回ゲーム完了時まで遅延`
- 対応内容（プレプロンプト追加分）:
  1. `lib/ui/screens/game/resultScreen.dart`の`_maybeRequestTrackingAuthorization()`に、`AppTrackingTransparency.requestTrackingAuthorization()`を呼ぶ直前に新規メソッド`_showAttPrePromptDialog()`を追加し、確定済み文言（タイトル「より快適に使うために」、本文・ボタン「次へ」は審査ログ確定文言のとおり）のプレプロンプトを表示するよう変更
  2. UIは`showDialog` + `AlertDialog`とし、`settingsScreen.dart`/`loginScreen.dart`の既存の確認・エラーダイアログ（`_deleteAccount`の削除確認、`_signOutAndShowError`）と同じ素の`AlertDialog`スタイルに合わせた（独自装飾は加えず、アプリ全体のダイアログトーンと統一）
  3. 決定事項どおり「次へ」ボタンのみとし、閉じる/キャンセル導線は設けていない。`barrierDismissible: false`に加え、`PopScope(canPop: false)`でバック相当の操作も抑止した
  4. `hasRequestedAtt`はプレプロンプト表示時点では更新せず、従来どおり実際のOSダイアログの結果（`requestTrackingAuthorization()`の戻り値）を受け取った時点でのみ`true`にするよう維持（変更なし）
  5. `initState()`内の呼び出しを`WidgetsBinding.instance.addPostFrameCallback`でラップし、初回フレーム描画後に`showDialog`が呼ばれるよう変更（`gameScreen.dart`の`_ensureSelectedCellVisible()`と同じ既存パターンを踏襲。初回描画前の`showDialog`呼び出しによるNavigator未接続リスクを回避するため）
  6. `docs/test_specs/consent_and_tracking_authorization.md`にプレプロンプト表示・「次へ」タップ・閉じる手段が無いことの確認・強制終了時の再表示などのテストケースを正常系・異常系・審査確認セクションに追加
  7. `flutter analyze`で対象ファイル（`resultScreen.dart`）にissueがないことを確認済み
- 関連コミット: `c04c7472 chore(ai-save): ATT許可ダイアログの直前に独自のプレプロンプトを追加`
- 反映状況: 反映済み（2026-07-29、`my_common_templates/advertising/admob_implementation_pattern.md`4番新規追加）※初版のタイミング変更分も含め、ATT運用パターンとして今回まとめて一般化

### 提案6への対応
- 状態: 対応済み
- 対応内容:
  1. `lib/utils/adMobBannerInterstitialAndRewardedAdManager.dart`の`AdManager`に前回インタースティシャル表示時刻を保持する`_lastInterstitialShownAt`と、90秒のクールダウン定数`_interstitialCooldown`を追加
  2. `showInterstitial()`で、前回表示から90秒未満の場合は広告を表示せず即座に`onDismissed`コールバックのみ呼ぶよう変更(画面遷移は妨げない)。90秒以上経過、または初回表示時は従来どおり表示し、表示時刻を更新する
  3. `docs/test_specs/ad_management.md`にクールダウン経過後の再表示・クールダウン中のスキップの正常系/異常系テストケースと、審査根拠（`advertising/ad_placement_ux_rules.md`項目2）を追記
  4. `flutter analyze`で対象ファイルにissueがないことを確認済み
- 追加で見つかった問題: なし
- 関連コミット: `1150b1e1 chore(ai-save): インタースティシャル広告に90秒のクールダウンを追加`
- 反映状況: 反映済み（2026-07-26, `my_common_templates/advertising/admob_implementation_pattern.md` 2番）※コード修正は未着手だが、あるべき設計（頻度キャップ）として先に一般化済み

### 提案7への対応
- 状態: 対応済み
- 対応内容:
  1. `functions/src/index.ts`の`deleteAccount`を修正し、`users/{uid}`の`recursiveDelete`と並行して`purchase_index`コレクションを`uid`フィールドでクエリし、該当する全エントリを`batch.delete`で削除するよう変更（該当エントリが0件でも`batch.commit()`は正常終了する）
  2. `docs/test_specs/in_app_purchase_and_receipt_verification.md`に本修正のテストケースを追加し、末尾に残っていた「既知の課題（`purchase_index`エントリを削除していない）」の記載を解消済みとして更新
  3. `npm run build`（`tsc`）でコンパイルエラーがないことを確認済み。Cloud Functionsのデプロイは本タスクのスコープ外のため未実施（ユーザー側で判断）
- 追加で見つかった問題: なし
- 関連コミット: `eedd5f87 chore(ai-save): deleteAccount実行時にpurchase_indexの孤立エントリも削除`
- 反映状況: 反映済み（2026-07-26, `my_common_templates/data_management/data_storage_pattern.md` 5番）※コード修正は未着手だが、逆引きコレクションはアカウント削除時に合わせて削除するという原則として先に一般化済み

### 提案8への対応
- 状態: 未着手

### 提案9への対応
- 状態: 対応済み（提案内容2「Android向け広告ユニットIDは当面現状維持」はユーザー承認済みの対応不要事項のため未実施）
- 対応内容:
  1. `.env.dev` `.env.prod` `.env.example`から、Remote Config移行（提案2）によりコード内で参照されなくなった4キー（`PRIVACY_POLICY_URL` `TERMS_OF_SERVICE_URL` `SUPPORT_EMAIL` `HOW_TO_SOLVE_URL`）を削除。削除前に`lib/`配下を`grep`し、`String.fromEnvironment`としての参照が残っていないことを確認済み（`lib/utils/remoteConfigUrlEmailValuesFetchService.dart`内に同名の文字列リテラルは残るが、これはFirebase Remote Configのパラメータキー名であり`.env`ファイルの参照ではないことを確認済み）
  2. Android向け広告ユニットIDは提案内容どおり対応不要のため変更なし
  3. `lib/utils/adMobBannerInterstitialAndRewardedAdManager.dart`の広告ユニットID6種（banner/interstitial/rewarded × Android/iOS）の`String.fromEnvironment(...)`すべてに、`.env.example`記載のGoogle公式公開テスト広告IDを`defaultValue`として設定
  4. `lib/main.dart`の`const String.fromEnvironment('GOOGLE_SIGN_IN_SERVER_CLIENT_ID')`を、`lib/utils/appWideUrlEmailAndProductIdConstants.dart`の`AppConstants.googleSignInServerClientId`（直書き定数）への参照に変更。`.env.dev`/`.env.prod`/`.env.example`から当該キーを削除。実装前に`.env.dev`/`.env.prod`の値を`grep`で確認したところ両ファイルとも同一値（`545986969793-013faqjkqg15udt8f4rp02sgj3g5f0ia.apps.googleusercontent.com`）だったため、確認中断は不要と判断しそのまま採用
  5. 上記に伴い`docs/test_specs/ad_management.md`（dart-define未指定時のdefaultValueフォールバックの正常系テストケース、Android広告IDが当面現状維持である旨の審査確認欄）と`docs/test_specs/login_and_authentication.md`（`serverClientId`直書き化の審査確認欄）を更新
  6. `flutter analyze`を対象ファイルおよびプロジェクト全体で実行し、新規issueがないことを確認済み（既存の無関係な警告1件＝`pubspec.yaml`の空`assets:`フィールドのみ残存、提案2対応時から変化なし）
  7. `flutter build ios --no-codesign --dart-define-from-file=.env.dev --simulator`でビルド確認済み（`✓ Built build/ios/iphonesimulator/Runner.app`）。`defaultValue`追加後のコードが正しくコンパイルされ、広告初期化まわりの構文・型エラーがないことを確認
- 確認したい点: なし（4番のdev/prod値は一致していたため中断不要と判断）
- 追加で見つかった問題: なし
- 関連コミット: `1fe7eafb chore(ai-save): dart-define値の設計見直し（死んだキー削除・広告IDのdefaultValue追加・GOOGLE_SIGN_IN_SERVER_CLIENT_IDの直書き化）`
- 反映状況: 反映済み（コード実装前に一般化済み）

### 提案10への対応
- 状態: 対応済み
- 対応内容:
  1. `.env.dev`ファイルを削除（元々`.gitignore`対象でGit管理外のため、削除自体はコミット差分には現れない）
  2. `.gitignore`から`.env.dev`の記載行を削除（`.env`・`.env.prod`の記載はそのまま維持）
  3. `.env.dev`へ言及していた以下のファイルを、「開発時は素の`flutter run`でよい（広告ユニットIDは`String.fromEnvironment`の`defaultValue`によりGoogle公式の公開テスト広告IDへ自動フォールバックするため）」という趣旨に書き換え
     - `CLAUDE.md`（2章の環境変数節。`.env.dev`/`.env.prod`併記だった記述を`.env.prod`のみの記述に整理し、`defaultValue`設定を必須要件として明記）
     - `docs/test_specs/ad_management.md`（前提条件の1文、および「6. 審査・コンプライアンス確認」内のAndroid広告ID現状維持に関する1文の計2箇所）
     - `docs/test_specs/select_mode_screen.md`（前提条件の1文）
  4. 着手前後で`grep -rn "\.env\.dev"`をプロジェクト全体に実行し、提案内容に明記された3ファイル以外に`.vscode/launch.json`（devデバッグ構成の`args`が`--dart-define-from-file=.env.dev`を指定しており、ファイル削除後は起動時にエラーとなる実質的な依存だったため）も見つかったため、同様の趣旨で`args`を撤去し素の`flutter run`相当の起動になるよう修正した。なお`.claude/settings.local.json`内の許可済みBashコマンドパターン（`sed 's/=.*/=<masked>/' .env.dev`）にも言及が残るが、これは過去に承認したコマンド許可リストの残骸であり、ドキュメント的な説明文でも機能的な依存でもないため対応対象外と判断し変更していない
  5. `.env.example`は`.env.prod`作成時のテンプレートとして変更せず維持
  6. `flutter analyze`を実行し、新規issueがないことを確認済み（既存の無関係な警告1件＝`pubspec.yaml`の空`assets:`フィールドのみ残存。提案2・9対応時から変化なし）
- 関連コミット: `685debc3 chore(ai-save): 冗長になった.env.devを削除し関連ドキュメント/設定を素のflutter run前提に修正`
- 反映状況: 該当なし（提案9の3番の帰結を適用しただけで新規の一般化事項なし）

### 提案11への対応
- 状態: 対応済み
- 対応内容:
  1. `ios/Runner.xcodeproj/project.pbxproj`の`TARGETED_DEVICE_FAMILY`をDebug/Release/Profileの3構成すべてで`"1,2"`から`"1"`（iPhoneのみ）に変更
  2. `ios/Runner/Info.plist`の`UISupportedInterfaceOrientations~ipad`キー（配列ごと）を削除
  3. 着手前に`grep -rn "ipad|iPad"`を`ios/`・`lib/`配下に実行したところ、提案対象の2ファイル以外に`ios/Runner/Assets.xcassets/AppIcon.appiconset/Contents.json`が`idiom:"ipad"`のアイコンエントリ13件（対応する実PNGファイルも実在）を保持していることが判明。iPad非対応化に伴い一緒に削除すべきか独断で判断せず中断・報告し、承認を得たうえで対応: `Contents.json`から`idiom:"ipad"`の13エントリを削除し、iPhone/ios-marketingのいずれとも共有していないiPad専用PNG9個（`Icon-App-20x20@1x.png` `Icon-App-40x40@1x.png` `Icon-App-50x50@1x.png` `Icon-App-50x50@2x.png` `Icon-App-72x72@1x.png` `Icon-App-72x72@2x.png` `Icon-App-76x76@1x.png` `Icon-App-76x76@2x.png` `Icon-App-83.5x83.5@2x.png`）を削除。iPhoneエントリとファイル名を共有する4ファイル（`Icon-App-20x20@2x.png` `Icon-App-29x29@1x.png` `Icon-App-29x29@2x.png` `Icon-App-40x40@2x.png`）はiPhone用として引き続き必要なため削除対象から除外。`pubspec.yaml`に`flutter_launcher_icons`が導入済みのため、将来iPad対応を再開する場合はコマンドで再生成可能なことを確認済み
  4. `lib/`配下では他にiPad前提の記述（専用レイアウト分岐・専用アセット等）は見つからなかった
  5. 上記調査・変更に着手する前、`ios/Runner.xcodeproj/project.pbxproj`・`ios/Runner/Info.plist`が本提案とは無関係な理由で既にuncommittedな状態だったため（Xcode上でユーザーが手動でiPhoneの向きを縦固定に変更した副作用と判明）、独断でまとめず中断・報告。ユーザー確認の結果、当該変更は意図的なもの（`lib/main.dart:57`の`SystemChrome.setPreferredOrientations([DeviceOrientation.portraitUp])`と整合）と判明したためそのまま維持しつつ、Xcode保存時に失われていた説明コメント2箇所を復元し、紛れ込んでいた未追跡のテスト用スクリーンショット（`flutter_01.png`）を削除したうえで、本提案とは別コミット（`4628cc80`）として先に切り離してコミット
  6. `flutter analyze`（新規issueなし、既存の無関係な警告1件のみ残存）、`plutil -lint ios/Runner/Info.plist`（OK）、`flutter build ios --no-codesign --simulator`（`✓ Built build/ios/iphonesimulator/Runner.app`、ビルド成功）で確認済み
- 関連コミット:
  - `4628cc80`（前処理: Xcode起因のiPhone縦向き固定化・コメント復元・不要スクリーンショット削除。提案11の変更範囲外だが、着手前のクリーンチェックで必要になったもの）
  - `7ac98ec0 chore(ai-save): iPhone専用（iPad非対応）への変更`（本提案の本体: TARGETED_DEVICE_FAMILY変更・`~ipad`キー削除・iPadアイコン削除）
- 反映状況: 該当なし（アプリごとの製品判断のため一般化しない）

### 提案12への対応
- 状態: 対応済み
- 対応内容（初版）:
  1. `ios/Runner.xcodeproj/project.pbxproj`のRunnerターゲットに、`[CP] Embed Pods Frameworks`（`FC4E394A6B0E91F9EAB51B29`）フェーズの直後に新規Run Scriptビルドフェーズ「Strip Simulator Archs From Embedded Frameworks」（`E0B00D68B907ACDB69E70551`）を追加した。挿入位置は提案どおり名目上の「Embed Frameworks」（`PBXCopyFilesBuildPhase`）の後だが、調査の結果このプロジェクトでは当該フェーズの`files`が空でCocoaPods由来のフレームワーク実体の埋め込みは`[CP] Embed Pods Frameworks`スクリプトフェーズが行っていることが判明したため、実際にフレームワークが`CODESIGNING_FOLDER_PATH`配下に存在する`[CP] Embed Pods Frameworks`の直後を挿入位置とした（名目上の「Embed Frameworks」直後だと埋め込み前でスクリプトが何も検出できないため）
  2. スクリプト内容: `PLATFORM_NAME`が`iphoneos`の場合のみ動作するようガードした上で、`${CODESIGNING_FOLDER_PATH}/Frameworks`配下の各`*.framework`を走査し、`lipo -info`でx86_64スライスの有無を確認、含まれていれば`lipo -remove x86_64`で除去後に`codesign --force --sign "${EXPANDED_CODE_SIGN_IDENTITY}" --preserve-metadata=identifier,entitlements,flags --timestamp=none`で再署名する（提案内容2の要件どおり、除去はx86_64のみでarm64は残し、除去後は再署名する）。シミュレータビルドでは即`exit 0`し何も変更しない
  3. `flutter build ipa --dart-define-from-file=.env.prod`で本番候補ビルドを再生成（`build/ios/archive/Runner.xcarchive` 506.6MB、`build/ios/ipa/math_practice.ipa` 52.2MB、いずれも成功）
  4. 生成された`.ipa`を展開し、`Payload/Runner.app/Frameworks/`配下の全42フレームワーク（`objective_c.framework`含む）に`lipo -info`を実行。修正前はfatファイルで`x86_64 arm64`の両方を含んでいた`objective_c.framework`が、修正後は`Architectures in the fat file: ... are: arm64`（x86_64を含まずarm64のみ）になっていることを確認。他の全フレームワークもx86_64を含まないことを確認済み（元々ほとんどが単一arch(arm64)のnon-fatファイルで、fatだったのは`objective_c.framework`のみだった）
  5. 再署名後の整合性も確認: `codesign -dv`で`objective_c.framework`が本アプリのTeamIdentifier（`4YQZKAY7HH`）で正しく再署名されていること、`codesign --verify --deep --strict`をアプリバンドル全体に実行しexit code 0（検証成功）であることを確認済み
  6. Transporter/`xcrun altool`相当の実検証（App Store Connect API Key等が必要）は未実施（提案内容4は「可能であれば」の任意項目のため、手順4・5のアーキテクチャ・署名検証で十分と判断）
  7. `flutter analyze`を実行し、新規issueがないことを確認済み（既存の無関係な警告1件＝`pubspec.yaml`の空`assets:`フィールドのみ残存、過去の対応から変化なし）
- 追加で見つかった問題: なし
- 関連コミット: `dbe669e7 chore(ai-save): Runnerターゲットにx86_64シミュレータarch除去のRun Scriptを追加`
- 対応内容（追加対応）:
  1. 原因調査どおり、直前まで実行していた`flutter build ios --simulator`によるシミュレータ向けビルドキャッシュ（DerivedData/Podsのビルド成果物）がデバイス向けアーカイブ時に誤って再利用されていた疑いを踏まえ、クリーンな状態から再ビルドした
  2. `flutter clean`実行後、`ios/Pods`・`ios/Podfile.lock`を削除。加えて`~/Library/Developer/Xcode/DerivedData/`配下を`info.plist`の`WorkspacePath`で照合し、本プロジェクトの`ios/Runner.xcworkspace`（`/Users/kazuk/Desktop/flutterprojects/Math_Practice/ios/Runner.xcworkspace`）に対応する`Runner-ewkhthqpwiswsrfslliatogrhljn`（直前のシミュレータビルドで更新されていたもの）のみを特定して削除（同名`Runner-*`が多数存在したが、`WorkspacePath`が一致しないもの・別クローンパス（`~/dev/Math_Practice/`）のものは対象外として削除していない）
  3. `flutter pub get`（`flutter clean`で失われた`ios/Flutter/Generated.xcconfig`の再生成のため）→ `cd ios && pod install --repo-update`を実行し、gRPC-Core（1.69.0）を含む58 Pods全てをクリーンな状態から再取得・再統合した
  4. `flutter build ipa --dart-define-from-file=.env.prod`で本番候補ビルドを再生成（`Runner.xcarchive` 507.8MB、`math_practice.ipa` 52.3MB、いずれも成功）
  5. 生成された`.ipa`を展開し、`Payload/Runner.app/Frameworks/`配下の埋め込み済み全45フレームワークのバイナリに対して`vtool -show-build <binary>`を実行して検証。`objective_c.framework`は`lipo -info`で`arm64`のみ（x86_64は含まない）、`vtool -show-build`で`platform IOS`（`IOSSIMULATOR`ではない）であることを確認。他の全フレームワークも同様に`arm64`単一アーキテクチャかつ`platform IOS`（`LC_BUILD_VERSION`を持たない9個のフレームワークは代わりに`LC_VERSION_MIN_IPHONEOS`＝デバイス専用の旧形式ロードコマンドを持つことを確認し、シミュレータ向けでないことを確認済み）であることを確認し、全フレームワークがデバイス向けであることを検証した
  6. `codesign --verify --deep --strict`をアプリバンドル全体に実行しexit code 0（検証成功）であることを確認済み
  7. 前回追加したRun Scriptビルドフェーズ（「Strip Simulator Archs From Embedded Frameworks」）はそのまま保険として残存させた（`project.pbxproj`内に存在することを確認済み）。今回の根本原因はplatform（シミュレータ向けビルド）であり同スクリプトはarchitecture（x86_64除去）しか見ていないため今回の問題自体は検出できないが、提案どおり保険としての位置づけのため変更は加えていない
  8. クリーンな`pod install`の副作用として、`ios/Podfile.lock`内の一部依存（`AppAuth` 2.0.0→2.1.0、`AppCheckCore` 11.2.0→11.3.1、`GoogleDataTransport` 10.1.0→10.1.1、`GoogleSignIn` 9.1.0→9.2.0、`GoogleUtilities` 8.1.0→8.1.2等）が、いずれも`Podfile`側の既存バージョン制約の範囲内で最新パッチ/マイナー版に更新された（`Podfile.lock`削除に伴う自然な再解決であり、`Podfile`自体は変更していない）
  9. `flutter analyze`を実行し、新規issueがないことを確認済み（既存の無関係な警告1件＝`pubspec.yaml`の空`assets:`フィールドのみ残存、過去の対応から変化なし）
  10. Transporter/`xcrun altool`相当の実アップロード検証（App Store Connect API Key等が必要）は未実施。本タスクのスコープは`vtool`によるplatform検証までのため、実際のTransporter再アップロードでの最終確認はユーザー側での実施が必要
- 関連コミット: `2e9dd189 chore(ai-save): クリーンビルドでobjective_c.frameworkのIOSSIMULATORプラットフォーム混入を解消`
- 反映状況: 反映済み（2026-08-10、`my_common_templates/flutter_project_structure.md` 8番新規追加・同日中に追加調査内容へ訂正済み。コード実装前に一般化済み）

## 提出後の審査結果
- （未提出）
