# flutterprojects 開発フロー・マニュアル

このドキュメントは `flutterprojects` で新しいアプリを開発する際の全体像を、人間向けに説明するマニュアルです。
AIエージェント向けの厳密なルールは各 `CLAUDE.md`（[`flutterprojects/CLAUDE.md`](./CLAUDE.md) と各プロジェクトの `CLAUDE.md`）に記載されているので、そちらが正となります。本ドキュメントはその要約・ガイドです。

## 1. 概要

新しいアプリのアイデアは、いきなり本番品質で作り始めるのではなく、次の4段階を経て育てていきます。

```
① 実験開発 → ② チームレビュー → ③ 構成書の作成 → 正規開発
```

- ①は自由な試作、③までは「本当に作るべきものを見極める」フェーズ、正規開発以降が「品質を担保しながら作る」フェーズです。
- フェーズが進むごとにClaudeに求める振る舞い（ペルソナ）も変わります。
- 人間が手で行うのは基本的に最初の起動だけで、`flutter create` などのセットアップ操作はすべてClaudeに実行させます（詳細は次章）。

## 2. 起動方法とトリガーワード

**人間が固定で行うのはこれだけです。**

```
cd flutterprojects && claude
```

CLAUDE.mdはcwdから上位ディレクトリへ再帰的に読み込まれるため、`flutterprojects` 直下（またはその配下）で `claude` を起動すれば、ルールが自動的にセッションへ読み込まれます。これ以降の `mkdir`・`flutter create`・作業ディレクトリへの `cd` などはすべてClaude自身が実行するので、人間が別途ターミナルでコマンドを打つ必要はありません。

起動後は、以下のキーワードを含めて依頼するだけで対応するフローが始まります。

| 言うこと | 始まるフロー |
|---|---|
| 「新規実験」＋アプリ名 | ①実験開発（`experiments/<アプリ名>/`をClaudeが作成して試作開始） |
| 「構成書作成」＋アプリ名 | ③構成書の初稿作成（①のログと②のレビュー内容から起こす） |
| 「新規プロジェクト作成」／「正規開発開始」＋アプリ名 | 正規開発の開始（`projectTemplate`の適用〜Git初期化まで） |

②チームレビューは人間同士のミーティングなので、対応するトリガーワードはありません。

## 3. 早見表

| フェーズ | 作業場所 | Claudeのペルソナ | 成果物 | 次フェーズへの引き継ぎ |
|---|---|---|---|---|
| ① 実験開発 | `experiments/<アプリ名>/` | 気軽な実験相棒（通常ルール免除） | 動くプロトタイプ＋`EXPERIMENT_LOG.md` | ②のレビュー資料 |
| ② チームレビュー | （ミーティング。Claudeは関与しない） | - | 合意された機能・改善アイデア | ③構成書の材料 |
| ③ 構成書の作成 | 未定（当面はMarkdown） | 構成書ドラフトを書く相棒 | 樹形図＋ノードごとの一言説明 | 正規開発のキックオフ資料 |
| 正規開発 | `<アプリ名>/`（独立したGitリポジトリ） | シニア・ネイティブアプリエンジニア 兼 厳格なQAテスター | 本番アプリ | リリース（`main`へのタグ付け） |

`projectTemplate/`（雛形）と `apple-reviewer/`（審査ナレッジベース）はフェーズそのものではなく、正規開発フェーズを支える参照専用フォルダです。

## 4. 各フォルダの役割とClaudeのペルソナ

- **`experiments/`** — 実験開発専用の置き場。`experiments/<アプリ名>/` ごとに自由な試作用Flutterプロジェクトを作ってよい。DRY原則・テスト仕様書・Git運用などの通常ルールは適用されない。ここでのClaudeは「細かい規律より試行速度」を優先する実験相棒として振る舞う。成果物はそのまま正規開発の土台にしない。
- **各プロジェクトフォルダ（正規開発）** — `<アプリ名>/CLAUDE.md`（`projectTemplate`を適用・カスタマイズしたもの）に従う独立したGitリポジトリ。ここでのClaudeは「世界最高レベルの技術力とApple/Googleの審査基準・法的コンプライアンスに精通したシニア・ネイティブアプリエンジニア 兼 厳格なQAテスター」として、SOP（意図確認→テスト仕様書作成→承認→実装→完了報告）を厳格に踏む。
- **`apple-reviewer/`** — App Store審査対策の参照専用ナレッジベース。独自の`CLAUDE.md`を持ち、審査エージェントとして振る舞う。通常のアプリ開発作業では読み書きしない。各プロジェクトとのやり取りは `review_logs/{アプリ名}/{日付}.md` を介して行う。
- **`projectTemplate/`** — 新規プロジェクト作成時に適用する雛形。中身は書き換えず、正規開発の開始時にコピーして使う。

## 5. フェーズ別の実務フロー

### ① 実験開発

`cd flutterprojects && claude` で起動したあと、「新規実験でXXXを作りたい」のように伝えるだけです。`experiments/<アプリ名>/` が無ければClaudeが `flutter create` で作成し、そのまま試作を始めます（人間が `flutter create` や `cd` を手で打つ必要はありません）。

- 画面遷移が破綻していても、ロジックが多少不正確でも、App Store審査基準に沿っていなくても構わない。とにかく試したい機能を動かしてみる。
- 唯一の記録義務は `EXPERIMENT_LOG.md`。1機能1エントリで、狙い・状態・気づいたことを**書ける範囲だけ**書けばよい（全項目任意）。Claudeが記録のために作業を止めたり質問したりすることはない。

### ② チームレビュー

- ①のプロトタイプと `EXPERIMENT_LOG.md` を持ってミーティングに臨む。
- 観点の例：実現可能性はあるか／機能を詰め込みすぎていないか／もっと良くするアイデアはないか。
- ここはClaudeが同席する工程ではなく、人間同士の意思決定の場。

### ③ 構成書の作成

「構成書作成、対象はXXX」のように伝えると、Claudeが①のログと②のレビュー内容をもとに初稿を起こします。

- レビューで採用が決まった機能を、樹形図＋各ノード1行程度の目的でまとめる。画面遷移だけでなく、ダイアログやトグルのようなその場で完結する分岐も枝として表現してよい。

  ```
  - ホーム画面
    - → モード選択画面
      - → 練習モード画面
      - → テストモード画面
    - → 設定画面（ダイアログ）
    - トグル：サウンドON/OFF（画面遷移なし）
  ```

- 独自の計算ロジックや細かい仕様まで書き込む必要はない。構成書は「完成した仕様書」ではなく、正規開発時にエージェントとの対話で詳細を詰めていくための出発点。
- 人間は初稿の枝の要不要を直す程度でよい。
- フォーマット（保存場所・記法）は今後改めて検討する予定。

### 正規開発への移行

構成書がまとまったら、「新規プロジェクト作成、アプリ名はXXX」のように伝えます。`flutterprojects` 直下で `flutter create` するところから `projectTemplate` の適用（`CLAUDE.md`・`.env.example`・テスト仕様書フォーマット・`PENDING_DECISIONS.md`・`_AI_INDEX.md`・ディレクトリ構成）、`git init` して `dev` ブランチで開発開始するところまで、すべてClaudeが実行します（詳細は `CLAUDE.md`「新規プロジェクト作成時のルール」を参照）。

## 6. 既存アプリへの機能追加の場合

既存アプリに新機能を追加する際、①〜③のフローを踏むかどうかは**機能の規模で判断**する。

- 既存のSOPで対応できる小さな修正・追加 → ①〜③を経由せず、該当プロジェクトの `CLAUDE.md` のSOPに直接従って着手してよい。
- 新しい画面群やアイデアの検証が必要な大きめの新機能 → まず `experiments/` で試作し、②のレビューを経て、③のミニ構成書（既存アプリへの追加分のみでよい）を作ってから組み込む。

## 7. 困ったときの参照先早見表

| 状況 | 参照先 |
|---|---|
| 実装を保留にしたい事項がある | 各プロジェクトの `docs/PENDING_DECISIONS.md` |
| App Store審査の指摘を確認・対応したい | `apple-reviewer/review_logs/{アプリ名}/{日付}.md` |
| 新機能実装前に既存ロジックを再利用できないか確認したい | 各プロジェクトの `lib/utils/_AI_INDEX.md` |
| テスト仕様を確認・更新したい | 各プロジェクトの `docs/test_specs/` |
| 実験段階で何を試したか振り返りたい | `experiments/<アプリ名>/EXPERIMENT_LOG.md` |

## 8. やってはいけないこと

- `experiments/` の成果物をそのまま正規プロジェクトのコードベースにコピーしない（正規開発は必ず `projectTemplate` からゼロで作る）。
- `projectTemplate/` 自体を直接編集しない（アプリ固有の修正はコピー先のプロジェクトでのみ行う）。
- `apple-reviewer/` 配下を通常の開発作業で編集しない（各プロジェクトからの対応は `review_logs/{アプリ名}/{日付}.md` の「対応サマリ」欄にのみ追記する）。
- `flutterprojects/CLAUDE.md`（ワークスペース全体のフロー）と、各プロジェクトの `CLAUDE.md`（そのアプリのSOP）を混同しない。
- 人間が `flutter create` や `cd` を手で実行しない。起動後の操作はClaudeに任せる。
